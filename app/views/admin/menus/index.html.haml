%h1 Menus

%ul
  -@menus.each do |menu|
    %li
      =menu.name
      =link_to "edit", edit_admin_menu_path(menu)
      %ul
        -menu.menu_items.each do |m|
          %li
            =link_to m.title, m.url

%p
  =link_to "new", new_admin_menu_path
